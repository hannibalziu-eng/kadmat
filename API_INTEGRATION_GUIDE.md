# 📖 دليل تكامل الخدمات - قصتك لفيديو\n\n## 🎯 نظرة عامة على النظام\n\nهذه الأداة تجمع بين أفضل الخدمات المجانية على الإنترنت لتحويل القصص والمانجا إلى فيديوهات احترافية بدون تكاليف.\n\n### الخدمات المستخدمة:\n\n| الخدمة | الاستخدام | الرابط | الحد المجاني |\n|--------|----------|--------|----------|\n| **Google Cloud TTS** | تعليق صوتي احترافي | https://cloud.google.com/text-to-speech | 1 مليون حرف/شهر |\n| **Hugging Face** | توليد الصور والنماذج | https://huggingface.co | مجاني بالكامل |\n| **AI Horde** | صور موّلدة بالـ AI | https://aihorde.net | 100% مجاني |\n| **LTX Studio** | تحويل الصور لفيديو | https://ltx.studio | مجاني محدود |\n| **DeepSeek** | تحليل النصوص الذكي | https://deepseek.com | مجاني |\n| **Stability AI** | صور عالية الجودة | https://platform.stability.ai | رصيد مجاني |\n\n---\n\n## 🚀 كيفية الاستخدام\n\n### المرحلة 1️⃣: تحليل القصة بالذكاء الاصطناعي\n\n```javascript\n// 1. أدخل نص القصة (100 كلمة على الأقل)\n// 2. اختر نوع المحتوى (قصة، مانجا، مقالة، حكاية خيالية)\n// 3. اضغط على زر \"تحليل القصة بـ AI\"\n```\n\n**ما الذي يحدث:**\n- تحليل النص واستخراج الأحداث الرئيسية\n- تحديد الشخصيات والمشاهد\n- وضع وصف بصري لكل مشهد\n- استخراج المشاعر والأجواء\n\n**النتيجة:**\n```\nنوع المحتوى: قصة\nعدد الكلمات: 250\nالمدة المتوقعة: 2-3 دقائق\n\n🎬 المشاهد الرئيسية:\n• بداية درامية وتحديد المكان والزمان\n• تطور الأحداث وظهور التحديات\n• ذروة الصراع والتشويق\n• الحل والنهاية السعيدة\n```\n\n---\n\n### المرحلة 2️⃣: توليد الصور\n\n#### خطوات التكامل مع Hugging Face:\n\n```javascript\n// احصل على API Key من: https://huggingface.co/settings/tokens\n\nconst API_KEY = \"your_hugging_face_token\";\nconst MODEL = \"stabilityai/stable-diffusion-xl-base-1.0\";\n\nasync function generateImages(prompt, count = 4) {\n    const images = [];\n    \n    for (let i = 0; i < count; i++) {\n        const response = await fetch(\n            \"https://api-inference.huggingface.co/models/\" + MODEL,\n            {\n                headers: { Authorization: `Bearer ${API_KEY}` },\n                method: \"POST\",\n                body: JSON.stringify({ inputs: prompt }),\n            }\n        );\n        \n        const result = await response.blob();\n        images.push(result);\n    }\n    \n    return images;\n}\n```\n\n#### استخدام AI Horde (بدون API Key):\n\n```javascript\n// لا يتطلب تسجيل - استخدم: apikey: 0000000000\n\nasync function generateImageAIHorde(prompt) {\n    // 1. إنشاء الطلب\n    const response = await fetch('https://aihorde.net/api/v2/generate/async', {\n        method: 'POST',\n        headers: {\n            'apikey': '0000000000',  // مفتاح عام\n            'Content-Type': 'application/json'\n        },\n        body: JSON.stringify({\n            prompt: prompt,\n            params: {\n                height: 512,\n                width: 512,\n                steps: 30\n            }\n        })\n    });\n    \n    const data = await response.json();\n    const jobId = data.id;\n    \n    // 2. انتظر النتيجة\n    let result;\n    while (!result) {\n        await new Promise(resolve => setTimeout(resolve, 5000));\n        const checkResponse = await fetch(`https://aihorde.net/api/v2/generate/check/${jobId}`);\n        const checkData = await checkResponse.json();\n        if (checkData.done) {\n            result = checkData.generations[0];\n        }\n    }\n    \n    return result.img; // base64 image\n}\n```\n\n#### أنماط الرسم المتاحة:\n\n- **واقعي**: صور فوتوغرافية واقعية\n- **أنمي**: أسلوب الرسوم المتحركة اليابانية\n- **كرتون**: رسوم كرتونية بسيطة وملونة\n- **3D**: رسومات ثلاثية الأبعاد\n- **رسم زيتي**: أسلوب الرسم الكلاسيكي\n- **ألوان مائية**: أسلوب الألوان المائية\n- **كوميك بوك**: أسلوب القصص المصورة\n\n---\n\n### المرحلة 3️⃣: التعليق الصوتي\n\n#### استخدام Google Cloud Text-to-Speech:\n\n```javascript\n// احصل على API Key من: https://console.cloud.google.com\n\nconst API_KEY = \"your_google_api_key\";\n\nasync function generateVoiceover(text, voice = \"ar-SA\", speed = 1.0) {\n    const response = await fetch(\n        `https://texttospeech.googleapis.com/v1/text:synthesize?key=${API_KEY}`,\n        {\n            method: 'POST',\n            headers: { 'Content-Type': 'application/json' },\n            body: JSON.stringify({\n                input: { text: text },\n                voice: {\n                    languageCode: voice,\n                    name: voice === \"ar-SA\" ? \"ar-SA-Standard-A\" : \"en-US-Standard-A\"\n                },\n                audioConfig: {\n                    audioEncoding: 'MP3',\n                    speakingRate: speed,\n                    pitch: 0\n                }\n            })\n        }\n    );\n    \n    const data = await response.json();\n    return data.audioContent; // base64 audio\n}\n```\n\n#### أصوات متاحة:\n\n**العربية:**\n- `ar-SA-Standard-A` - ذكر (الأساسي)\n- `ar-SA-Standard-B` - ذكر (بديل)\n- `ar-SA-Standard-C` - أنثى (الأساسي)\n- `ar-SA-Standard-D` - أنثى (بديل)\n\n**الإنجليزية:**\n- `en-US-Standard-A` - ذكر\n- `en-US-Standard-C` - أنثى\n- `en-US-Neural2-A` - ذكر (محسّن)\n- `en-US-Neural2-C` - أنثى (محسّن)\n\n#### السرعات المتاحة:\n- `0.8` - بطيء\n- `1.0` - عادي\n- `1.2` - سريع\n- `1.4` - سريع جداً\n\n---\n\n### المرحلة 4️⃣: إنشاء الفيديو النهائي\n\n#### استخدام FFmpeg لدمج العناصر:\n\n```javascript\n// للاستخدام في الويب، نستخدم FFmpeg.js\n// npm install @ffmpeg/ffmpeg @ffmpeg/util\n\nimport { FFmpeg, toBlobURL } from \"@ffmpeg/ffmpeg\";\nimport { fetchFile } from \"@ffmpeg/util\";\n\nconst ffmpeg = new FFmpeg();\n\nasync function createVideo(images, audio, musicFile, quality = \"1080p\") {\n    const baseURL = 'https://cdn.jsdelivr.net/npm/@ffmpeg/core@0.12.4/dist/umd';\n    \n    await ffmpeg.load({\n        coreURL: await toBlobURL(`${baseURL}/ffmpeg-core.js`, 'text/javascript'),\n        wasmURL: await toBlobURL(`${baseURL}/ffmpeg-core.wasm`, 'application/wasm'),\n    });\n    \n    // 1. حفظ الملفات المؤقتة\n    for (let i = 0; i < images.length; i++) {\n        await ffmpeg.writeFile(`image${i}.jpg`, await fetchFile(images[i]));\n    }\n    await ffmpeg.writeFile('audio.mp3', await fetchFile(audio));\n    if (musicFile) {\n        await ffmpeg.writeFile('music.mp3', await fetchFile(musicFile));\n    }\n    \n    // 2. إنشاء فيديو من الصور\n    const duration = audio ? getAudioDuration(audio) : 30;\n    const imageDuration = (duration / images.length).toFixed(2);\n    \n    // 3. دمج الصوت والموسيقى\n    const cmd = [\n        '-f', 'concat',\n        '-safe', '0',\n        '-i', 'images_list.txt',\n        '-i', 'audio.mp3'\n    ];\n    \n    if (musicFile) {\n        cmd.push('-i', 'music.mp3');\n    }\n    \n    cmd.push(\n        '-c:v', 'libx264',\n        '-r', '30',\n        '-pix_fmt', 'yuv420p',\n        '-vf', `scale=${getResolution(quality)}`,\n        '-c:a', 'aac',\n        'output.mp4'\n    );\n    \n    await ffmpeg.exec(cmd);\n    \n    const data = await ffmpeg.readFile('output.mp4');\n    return new Blob([data.buffer], { type: 'video/mp4' });\n}\n\nfunction getResolution(quality) {\n    const resolutions = {\n        '720p': '1280:720',\n        '1080p': '1920:1080',\n        '4k': '3840:2160'\n    };\n    return resolutions[quality] || '1920:1080';\n}\n```\n\n---\n\n## 🔧 خيارات الموسيقى المتاحة:\n\n| النوع | الوصف | الاستخدام |\n|--------|--------|----------|\n| **درامي** | موسيقى درامية مشبوهة | للقصص الدرامية والروايات |\n| **سينمائي** | موسيقى سينمائية احترافية | للأفلام والمحتوى الاحترافي |\n| **مغامرة** | موسيقى مغامرة وحركة | للقصص المليئة بالأحداث |\n| **هادئ** | موسيقى هادئة وسلام | للقصص الرومانسية والحزينة |\n| **مرح** | موسيقى مرحة وإيجابية | للقصص الفكاهية والخفيفة |\n| **غامض** | موسيقى غامضة وتشويق | للقصص البوليسية والغامضة |\n| **رومانسي** | موسيقى حب وعاطفة | للقصص الرومانسية |\n| **مخيف** | موسيقى رعب وتخويف | لقصص الرعب والخوف |\n| **ملحمي** | موسيقى ملحمية وأسطورية | للقصص الخيالية والملاحم |\n\n---\n\n## 🎬 أمثلة عملية\n\n### مثال 1: تحويل مقالة إلى فيديو\n\n```javascript\nconst storyText = `\nفي قرية صغيرة بعيدة عن المدينة، كان هناك صبي يحلم بأن يصبح معلماً.\nقرر أن ينتقل إلى المدينة ليحقق حلمه رغم معارضة والديه.\nبعد سنوات من الجهد والعمل الشاق، أصبح معلماً ناجحاً.\nعاد إلى قريته وأسس مدرسة لتعليم أطفال القرية.\n`;\n\n// 1. تحليل القصة\nconst analysis = await analyzeStory(storyText, 'article');\nconsole.log(analysis); // نتائج التحليل\n\n// 2. توليد 4 صور\nconst images = await generateImages(analysis.scenes, 4);\n\n// 3. إنشاء التعليق الصوتي\nconst voiceover = await generateVoiceover(analysis.narrative, 'ar-SA', 1.0);\n\n// 4. إنشاء الفيديو\nconst video = await createVideo(images, voiceover, 'dramatic', '1080p');\n\n// 5. حفظ الفيديو\nconst blob = video;\nconst url = URL.createObjectURL(blob);\nconst link = document.createElement('a');\nlink.href = url;\nlink.download = 'my_story.mp4';\nlink.click();\n```\n\n### مثال 2: تحويل مانجا إلى فيديو\n\n```javascript\nconst mangaPages = [\n    \"صفحة المانجا الأولى - بطل يستيقظ في الصباح\",\n    \"صفحة المانجا الثانية - يقابل منافسه\",\n    \"صفحة المانجا الثالثة - معركة مثيرة\",\n    \"صفحة المانجا الرابعة - الانتصار النهائي\"\n];\n\n// توليد صور بأسلوب أنمي\nconst animeImages = await Promise.all(\n    mangaPages.map(page => \n        generateImages(`${page}, anime style, detailed, high quality`, 1)\n    )\n);\n\n// إنشاء تعليق صوتي بصوت أنثى\nconst voiceover = await generateVoiceover(\n    \"قصة المانجا الرائعة\",\n    'ar-SA',\n    1.0\n);\n\n// إنشاء فيديو بموسيقى ملحمية\nconst video = await createVideo(\n    animeImages,\n    voiceover,\n    'epic',\n    '1080p'\n);\n```\n\n---\n\n## ⚡ نصائح الأداء\n\n### لتسريع عملية التوليد:\n\n1. **استخدم جودة 720p** للمعاينة السريعة\n2. **قلل عدد الصور** - 3-4 صور كافية\n3. **استخدم نص القصة الأقصر** - ملخص بدلاً من الكل\n4. **استخدم الذاكرة المؤقتة** - احفظ النتائج\n\n### لتحسين الجودة:\n\n1. **استخدم جودة 1080p أو 4K** للمحتوى النهائي\n2. **أضف وصفاً بصرياً تفصيلياً** للصور\n3. **استخدم أصوات محسّنة** (Neural voices)\n4. **اختر موسيقى مناسبة** للقصة\n\n---\n\n## 🐛 حل المشاكل الشائعة\n\n### المشكلة: \"API Key غير صحيح\"\n**الحل:**\n- تحقق من مفتاح API من لوحة التحكم\n- تأكد من تفعيل الخدمة\n- تحقق من صلاحيات المفتاح\n\n### المشكلة: \"الصور بطيئة في التوليد\"\n**الحل:**\n- استخدم AI Horde (مجاني بدون قيود)\n- قلل حجم الصور\n- استخدم نموذج أسرع\n\n### المشكلة: \"الفيديو يتأخر\"\n**الحل:**\n- استخدم جودة أقل (720p)\n- قلل عدد الصور\n- استخدم FFmpeg.wasm بدلاً من الإصدار الكامل\n\n---\n\n## 📚 المراجع\n\n- [Google Cloud Text-to-Speech](https://cloud.google.com/text-to-speech/docs)\n- [Hugging Face API](https://huggingface.co/docs/api-inference/)\n- [AI Horde Documentation](https://aihorde.net/api)\n- [FFmpeg.js](https://github.com/ffmpegwasm/ffmpeg.wasm)\n- [Stability AI](https://platform.stability.ai/docs/getting-started)\n\n---\n\n## 📞 الدعم والمساعدة\n\nللمزيد من المساعدة والأسئلة:\n- قم بزيارة مجتمع المطورين\n- اقرأ التوثيق الكامل\n- جرب الأمثلة العملية\n\n**تم إنشاؤها بواسطة:** Ai Video Generator Team\n**آخر تحديث:** يناير 2025\n**الإصدار:** 1.0.0\n"